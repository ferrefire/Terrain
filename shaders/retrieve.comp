#version 460

#extension GL_GOOGLE_include_directive : require

#include "variables.glsl"

#include "terrainFunctions.glsl"

layout(set = 1, binding = 0, std430) buffer RetrieveData 
{ 
	float viewTerrainHeight;
} retrieveData;

layout(local_size_x = 1, local_size_y = 1) in;

void main()
{
	retrieveData.viewTerrainHeight = TerrainValues(variables.viewPosition.xz).x * maxHeight;
}